# 📢 Photo Watermark v1.7.1 — Release Notes / 更新公告

**EN:** This is a UI bug-fix release. Photo list thumbnails now display correctly; the checkmark (☑/☐) no longer overlaps or misaligns with the thumbnail. In the preview, you can now drag to reposition the image after zooming in (pan), and the mouse wheel zooms in fine steps (0.25x per notch, 0.25x–4.0x; from Fit, wheel-up first goes to 100%). Dragging the watermark is now real-time (no more lag): previews are rendered on a scaled base image, so even large photos follow the cursor smoothly.

**中文：** 本次为界面修复版。照片列表缩略图恢复正常显示；勾选符号（☑/☐）不再与缩略图重叠、错位。预览放大后可以按住拖拽调整显示位置（平移画布）；鼠标滚轮可逐级缩放（每格 0.25 倍，范围 0.25x–4.0x；fit 状态下向上滚先到 100%）。水印拖拽改为实时跟手：预览在缩放底图上绘制水印，大图拖拽也不再卡顿。

## What's Fixed / 修复内容
- Fix photo-list thumbnails not displaying / 修复照片列表缩略图无法显示
- Fix checkmark & thumbnail overlap / misalignment / 修复勾选符号与缩略图重叠、错位
- Drag to pan the preview after zooming / 预览放大后支持拖拽平移
- Mouse-wheel zoom with fine steps / 鼠标滚轮逐级缩放
- Real-time watermark drag (faster preview rendering) / 水印拖拽实时跟手（预览渲染提速）

See CHANGELOG.md / 详见 CHANGELOG.md。
