# 更新日志 (Changelog)

本项目严格遵循 [语义化版本 SemVer](https://semver.org/lang/zh-CN/)：

- **主版本 X**：破坏性变更，不兼容旧版本（API/架构/配置不兼容）。
- **次版本 Y**：新增功能，向下兼容。
- **修订版本 Z**：只修 bug / 安全修复，无新功能，完全兼容。

---

## [1.4.0] - 2026-08-26

### 新增
- 插件系统新增 `api.on_ui_ready(func)` 扩展点：主界面构建完成后把 App 实例交给插件，
  插件可向主界面添加/修改任意控件（顶部横幅、导出页按钮、改标题、操作状态栏等）
  - 仅启动时调用一次，UI 改动需重启生效
  - 单个插件异常不阻断其他插件，异常写入 pwm.log
- PLUGINS.md 增加 on_ui_ready 文档；示例插件见 plugin-repos/ui-booster/

---

## [1.3.3] - 2026-08-26

### 修复（插件安装流程）
- 插件下载后按清单 checksum 校验，不一致拒绝安装；清单未提供 checksum 时弹窗提示"来源可信度未知"
- 已安装但加载失败的插件：商店显示红色「加载失败」状态，可一键「更新 / 重装」自愈
  （已安装判定改为 安装记录 ∪ 已加载插件）
- 覆盖安装改为原子提交（staging → .bak → 新目录），任一步失败自动恢复旧版本
- zip 解压增加大小/数量防护（总量 300MB、2000 个文件），防 zip 炸弹
- 防止重复点击安装并发冲突：安装中禁用按钮并显示「安装中…」
- 修复安装/加载失败提示回调捕获异常变量导致状态栏不显示的问题

### 其他
- RELEASE.md 增加"发布插件必须同步 checksum"的流程说明

---

## [1.3.2] - 2026-08-26

### 新增
- 插件商店「更新检测」：
  - 比对商店版本号与本地已装插件版本号，有新版本时显示「更新」按钮（一键覆盖升级）
  - 支持 checksum（发布 zip 的 SHA-256）检测：即使版本号不变，只要发布内容有变化也会提示更新
  - 插件卡片显示更新时间（updated_at）
  - 安装时记录 版本 / 校验和 / 安装时间（plugins/.installed.json）
- 插件商店界面补齐 简体中文 / English / 繁體中文 翻译

### 安全修复
- 修复插件安装 Zip Slip 路径穿越漏洞：解压前校验压缩包内每个文件的路径，
  拒绝 `../`、绝对路径、盘符（`C:`）等非法路径，恶意插件包无法把文件写到 plugins/ 之外
  （商店安装 + 手动添加插件两条路径都已修复）
- 插件商店地址 / 下载地址仅支持 HTTPS（plugin_store_url / install_url 为 http:// 时直接拦截并提示）

### 修复
- checksum 比对大小写规范化：清单里大写 / 小写哈希不再误判为"有更新"
- 版本号比较支持 `v` 前缀（`v1.1.0` 不再被误判为低于 `1.0.0`）

### 新增（主程序热更新）
- 主程序热更新同样支持 checksum 检测：update.json 增加 checksum（exe 的 SHA-256），
  即使版本号不变，只要发布内容变了也会提示更新（覆盖发布同版本号时用户也能收到更新）
- 下载完成后按清单 checksum 校验文件，避免损坏/被篡改的下载文件覆盖 exe

---

## [1.3.1] - 2026-08-25

### 修复
- 修复自动更新失败（Security validation failure: parent process has different executable）
  - 更新时清除 PyInstaller 运行时环境变量（_PYI_*），新程序不再被误判为 onefile 子进程而拒绝启动
  - 更新脚本新增错误日志 update_error.log，方便排查
- 修正 v1.3.0 发布包内程序与 v1.2.0 相同的问题（已重新打包）

### 验证
- v1.3.1 的自动更新流程已本地端到端验证通过（检查更新 → 下载 → 替换 → 重启成功）

### 注意（重要）
- **v1.2.0 / v1.3.0 用户请手动下载 v1.3.1 一次**：旧版本程序内的自动更新逻辑存在缺陷
  （未清除 PyInstaller 运行时环境变量），无法自动升级到本版。安装 v1.3.1 后，
  之后的自动更新即恢复正常。

---

## [1.3.0] - 2026-08-25

### 新增
- 内置「插件商店」窗口（「导出」页 →「插件商店」）：
  - 读取主仓库 plugins.json 插件目录
  - 展示插件名称/版本/作者/说明
  - 一键下载安装到 plugins/ 目录，自动刷新加载
- 插件目录 plugins.json（schema_version 1）
- 插件仓库统一加 pwm-plugin 发现标签

---
## [1.2.0] - 2026-08-25（当前）

### 新增
- 插件设置项 API：api.add_setting(key, label, kind, default, options)
  （text / file / number / select / bool / range 滑块）
- 「插件设置」窗口：插件注册的设置项在此调整，保存到 config.json，重启保留
- 插件设置实时预览：滑块 / 文件 / 下拉 / 输入改动即时刷新预览
- 文字水印 + 图片水印可同时存在（插件水印样式叠加在文字水印之上）
- 图片水印插件：自定义图片（PNG/JPG/GIF，动图取第一帧静态）+ 10 张内置预设 GIF 水印
- 预设水印以缩略图画廊显示，点击选择
- 字体：支持子文件夹扫描、「刷新字体」按钮、打开下拉自动刷新、损坏字体自动回退
- 插件独立仓库模式（插件不再随本体发布，自由下载安装）
- 插件版本号机制：插件管理窗口显示每个插件的版本

### 修复
- 预设水印图缩略图点击无效（lambda 闭包变量捕获）
- 字体识别相关优化
- 自动更新替换失败（“Security validation failure”）：改用 VBS + ShellExecute，不再经过 cmd

### 其他
- 发布包 / 插件安装包按版本命名，releases/vX.Y.Z/ 分目录

---

## [1.1.0] - 2026-08-25

### 新增
- 插件 API 扩展（更强的可扩展性）：
  - `api.add_template_preset(name, template)` —— 插件可新增模板预设
  - `api.add_watermark_style(name, label, renderer)` —— 插件可自定义水印渲染样式
  - `api.on_export(func)` —— 导出前处理钩子（水印后、保存前可修改图像）
- 「样式」页新增「水印样式」下拉框（默认样式 + 插件样式）
- 示例插件更新：演示全部 6 种插件扩展点

---

## [1.0.0] - 2026-08-25（首个正式发布）
 - 2026-08-25（首个正式发布）

首个公开版本，包含以下全部功能：

### 功能
- 自动识别相机品牌型号（SONY ILCE-7CM2 → Sony A7C II，可手动覆盖）
- 自动检测快门 / 光圈 / ISO / 焦距 / 镜头 / 拍摄时间
- 支持 RAW（ARW/NEF/CR2/DNG 等，内嵌全尺寸预览，竖拍自动转正）
- 水印位置（九宫格 + 偏移 + 边距，默认贴底）/ 字号 / 颜色 / 字体 / 背景条 / 描边 / 阴影
- 模板预设 + 自定义模板（可保存），变量 {make} {model} {shutter} {aperture} {iso} 等
- 批量导出 JPG / PNG / WebP / BMP，JPG 可保留 EXIF
- 三种语言：简体中文 / English / 繁體中文（「导出」页一键切换）
- 自定义字体（fonts/ 文件夹或界面添加）
- 插件系统 + 插件管理窗口（自定义变量 / 相机名 / 导出格式）
- 热更新检查（config.json 配置 update_url）
- 设置自动保存（config.json）

### 其他
- 窗口标题显示版本号（Photo Watermark v1.0.0）
- 开源发布（MIT 许可证；作者 Shiraijikuu · AI 协助 OpenAI Codex）
