# 更新日志 (Changelog)

本项目严格遵循 [语义化版本 SemVer](https://semver.org/lang/zh-CN/)：

- **主版本 X**：破坏性变更，不兼容旧版本（API/架构/配置不兼容）。
- **次版本 Y**：新增功能，向下兼容。
- **修订版本 Z**：只修 bug / 安全修复，无新功能，完全兼容。

---

## [2.0.0] - 2026-08-28

### 新增
- 水印样式 API 支持 replaces_watermark（整图重绘型）：add_watermark_style 入口统一三元组，
  旧 3 参插件自动兼容（图片水印插件路径不变）；新增 _call_style_renderer 按参数个数兼容新老 renderer
- 「模糊卡片」水印样式插件支持：模糊深色背景 + 中央清晰前景（圆角/阴影/描边）+ 底部相机参数信息栏
  （深色底纹、字号自适应防溢出）；文字水印只画在模糊背景，前景区域绝不落字
- 整图重绘样式 + 兼容叠加样式共存：模糊卡片渲染后自动叠加图片水印（品牌 logo 等），
  「模糊卡片 + 相机品牌 logo」可同时出现，logo 带大小/位置/旋转/透明度滑块
- 相机品牌 logo 预设（10 品牌 × 白/透明版，2048px）加入图片水印插件（canon/sony/nikon/fujifilm/
  hasselblad/leica/olympus/panasonic/pentax/ricoh），可与文字水印同屏叠加
- 主程序预加载 PIL.ImageEnhance，确保打包版 exe 内运行时加载的插件可用
- UI 重构为三栏布局（左=照片列表 / 中=预览 / 右=水印参数），顶部输入/输出分组、底部状态栏
- 主题系统升级：8 套精选配色 + 跟随系统（theme-switcher v2.0.0），修复黑夜模式菜单栏白底、
  三栏语义色、竖排 Notebook、hover 色阶、一次性轻动效
- 「导出」tab 插件设置改为下拉：逐插件独立设置窗 + 汇总窗兜底；顶部「插件」菜单列出各插件

---
## [1.9.0] - 2026-08-27

### 新增
- 更新检测 / 插件商店「双源回退」：jsDelivr @main 存在分支解析缓存、可能长时间滞后于最新提交时，
  运行时自动改请求 GitHub raw 权威源（_fetch_json_best / _fetch_json_store）：
  - 更新检测：raw 版本号更高则采用 raw 数据，raw 失败回退 jsDelivr（对存量用户即时生效，无需改配置）
  - 插件商店：优先 raw 最新列表，raw 失败回退 jsDelivr
  - 非 jsDelivr @main 的自定义源仍只请求单源

### 优化
- 预览输出尺寸上限：大图放大到超出画布过扫描（1.25x）时只渲染「可见区域」裁剪图，
  不再渲染 scale x 全图（如 6000x4000 @ 4x = 24000x16000），避免内存暴涨与卡顿
- 预览缩放算法性能优化：缩小用 BOX（快 3-4 倍）、放大用 BILINEAR（快约 2 倍），
  大图滚轮/滑块缩放显著提速（实测 100% 447ms→14ms、200% 1.8s→11ms）
- 滑块连续拖动改为 80ms 节流合并渲染，拖动不逐事件全量重渲染
- 裁剪预览图按全图坐标定位水印（render_watermark 支持 full_size/origin），
  放大预览中水印位置与全图严格一致（逐像素验证通过）

### 修复
- 修复放大钳制模式下拖拽反向跳动：平移超出渲染缓冲触发"窗口跟随"时方向写反
  （item 右移露左侧却把窗口右移）。现改为窗口随内容反向移动保持视觉连续
  （vx -= pan_x/scale），拖拽方向与鼠标一致
- 修复放大钳制模式拖拽平滑：缓冲重渲染后 pan 归零但拖拽起点未同步，
  导致下一次拖拽从旧起点计算、位移瞬间叠加、图像突然加速。
  现重渲染时同步 _pan_start/_drag_start 到当前鼠标位置，拖拽完全跟随鼠标
  （实测 200px 连续拖拽中无任何视觉跳变）

---
## [1.8.0] - 2026-08-27

### 新增
- 预览缩放滑块（25%~400%，连续）+ 当前百分比标签：与滚轮、按钮（适应窗口/100%/200%）双向同步；
  滑块以画面中心为锚连续缩放；fit 状态显示真实百分比（低于 25% 时滑块贴最左、标签仍显示实际值）

### 修复
- 修复滚轮 fit 边界 bug：大图 fit 实际比例 < 0.25 时，向下滚不再反向放大到 0.25，而是保持「适应窗口」
- README「从源码打包 exe」命令补上 --hidden-import windnd 与 --collect-all tkinterdnd2（与 RELEASE.md 一致）

---
## [1.7.2] - 2026-08-27

### 修复
- 修复拖拽照片文件夹失效：windnd 在 64 位 Python 下回调访问冲突（每次窗口消息都崩溃、拖放不识别）。
  改用 tkinterdnd2（自带 tkdnd 原生库，64 位稳定）作为首选，windnd 仅作回退；
  拖拽不可用时空状态引导不再提示"拖进来"，改为"点上方选择文件夹"
- 修复滚轮缩放无法回到"适应窗口"：最小档 0.25 再往下滚 -> 回到 fit，不再卡死
- 修复滚轮缩放时画面跳回中心：改为以鼠标为中心缩放（保持鼠标下的图像点不动）
- 滚轮缩放更平滑：fit 状态下向上/向下各到最近的 0.25 档，不再从 0.3 直接蹦到 100%
- 修复主题下插件预设缩略图（gallery）选中蓝色高亮在窗口刚打开时被主题覆盖：
  创建时即打 _wm_keep_bg 标记（不再只有点击后才恢复）

---
## [1.7.1] - 2026-08-27

### 修复
- 修复照片列表缩略图无法显示：缩略图消息从 3 元组改为 2 元组，轮询崩溃不再停掉整个列表刷新
- 修复勾选符号（☑/☐）与缩略图重叠、未对齐：调整列表列宽与对齐方式
- 预览大图缩放后可以按住拖拽调整显示位置（平移画布，实时跟手）
- 预览缩放支持鼠标滚轮逐级调整（0.25 步进，范围 0.25x~4.0x；fit 时向上先到 100%）
- 水印拖拽改为实时预览：拖拽过程不再反复写配置，按帧率渲染；预览改为在缩放底图上绘制水印，
  大幅提升大图拖拽响应速度

---
## [1.7.0] - 2026-08-27

### 修复
- 修复动态窗口（插件设置/管理/商店）主题适配的时机 bug：on_window_created 改为
  窗口 build 完成后（after(0)）再通知，主题插件能给到真实内容上色，不再残留默认白底
- 缩略图画廊：选中高亮（蓝色块）不再被主题插件覆盖（控件标记 _wm_keep_bg，主题跳过）

### 新增
- 拖拽支持：把照片文件夹（或图片文件）直接拖进主窗口即可扫描加载（Windows，基于 windnd）

---

## [1.6.0] - 2026-08-27

### 修复
- 修复语言切换重启报错 "Failed to start embedded python interpreter!"：
  重启前未清除 PyInstaller onefile 运行时环境变量（_PYI_*），新 exe 被引导器误判为子进程。
  已抽公共函数 _clean_pyi_env()，热更新与重启共用，避免再漏。

### 新增
- 插件系统新增 api.on_window_created(func) 扩展点：动态 Toplevel（插件设置/管理/商店等）
  创建完成后把窗口交给插件（如给新窗口上主题色）。
- theme-switcher 插件升级适配：动态窗口也随主题变色。

---

## [1.5.0] - 2026-08-27

### 新增（UI 改版）
- 照片列表：搜索 / 排序（文件名·拍摄时间·相机）/ 筛选（全部·RAW·JPG）/ 缩略图（懒加载+分批+占位兜底）
- 预览缩放：适应窗口 / 100% / 200%；按住对比原图（不画水印层）
- 水印拖拽定位：在预览画布上直接拖动水印，实时联动「位置」页 9 宫格与 X/Y 偏移
- 导出进度增强：逐文件 x/N 状态、完成汇总（成功/失败/跳过 + 失败明细）、底部「打开输出文件夹」按钮
- 空状态引导：未加载照片时预览区居中显示操作提示

### 其他
- 主题插件 theme-switcher 适配新版界面（工具栏 / 缩放按钮等随主题变色）

---

## [1.4.1] - 2026-08-26

### 修复
- 修复"检测不到更新"：raw.githubusercontent.com 的 CDN 缓存会导致插件商店 / 热更新清单延迟（最长可超过 5 分钟）
  - 默认清单地址改为 jsDelivr CDN（GitHub 官方认可的 CDN，推送即生效）
  - 已配置旧版 raw 默认地址的配置自动迁移到 jsDelivr；自定义地址不受影响
- 降低杀毒软件误报：打包程序嵌入完整版本信息（公司/产品名/文件描述/版权/版本号），
  无签名、无元数据的 PyInstaller 单文件程序最容易被 Defender 启发式误报
- README 增加「杀毒软件误报」处理指南（解除锁定 / Defender 排除 / 向微软提交误报）

### 其他
- RELEASE.md 构建命令加入 --version-file（版本信息由版本号自动生成）

---

## [1.4.0] - 2026-08-26

### 新增
- 插件系统新增 `api.on_ui_ready(func)` 扩展点：主界面构建完成后把 App 实例交给插件，
  插件可向主界面添加/修改任意控件（顶部横幅、导出页按钮、改标题、操作状态栏等）
  - 仅启动时调用一次，UI 改动需重启生效
  - 单个插件异常不阻断其他插件，异常写入 pwm.log
- PLUGINS.md 增加 on_ui_ready 文档；示例插件见 plugin-repos/ui-booster/

---

## [1.3.3] - 2026-08-26

### 修复（插件安装流程）
- 插件下载后按清单 checksum 校验，不一致拒绝安装；清单未提供 checksum 时弹窗提示"来源可信度未知"
- 已安装但加载失败的插件：商店显示红色「加载失败」状态，可一键「更新 / 重装」自愈
  （已安装判定改为 安装记录 ∪ 已加载插件）
- 覆盖安装改为原子提交（staging → .bak → 新目录），任一步失败自动恢复旧版本
- zip 解压增加大小/数量防护（总量 300MB、2000 个文件），防 zip 炸弹
- 防止重复点击安装并发冲突：安装中禁用按钮并显示「安装中…」
- 修复安装/加载失败提示回调捕获异常变量导致状态栏不显示的问题

### 其他
- RELEASE.md 增加"发布插件必须同步 checksum"的流程说明

---

## [1.3.2] - 2026-08-26

### 新增
- 插件商店「更新检测」：
  - 比对商店版本号与本地已装插件版本号，有新版本时显示「更新」按钮（一键覆盖升级）
  - 支持 checksum（发布 zip 的 SHA-256）检测：即使版本号不变，只要发布内容有变化也会提示更新
  - 插件卡片显示更新时间（updated_at）
  - 安装时记录 版本 / 校验和 / 安装时间（plugins/.installed.json）
- 插件商店界面补齐 简体中文 / English / 繁體中文 翻译

### 安全修复
- 修复插件安装 Zip Slip 路径穿越漏洞：解压前校验压缩包内每个文件的路径，
  拒绝 `../`、绝对路径、盘符（`C:`）等非法路径，恶意插件包无法把文件写到 plugins/ 之外
  （商店安装 + 手动添加插件两条路径都已修复）
- 插件商店地址 / 下载地址仅支持 HTTPS（plugin_store_url / install_url 为 http:// 时直接拦截并提示）

### 修复
- checksum 比对大小写规范化：清单里大写 / 小写哈希不再误判为"有更新"
- 版本号比较支持 `v` 前缀（`v1.1.0` 不再被误判为低于 `1.0.0`）

### 新增（主程序热更新）
- 主程序热更新同样支持 checksum 检测：update.json 增加 checksum（exe 的 SHA-256），
  即使版本号不变，只要发布内容变了也会提示更新（覆盖发布同版本号时用户也能收到更新）
- 下载完成后按清单 checksum 校验文件，避免损坏/被篡改的下载文件覆盖 exe

---

## [1.3.1] - 2026-08-25

### 修复
- 修复自动更新失败（Security validation failure: parent process has different executable）
  - 更新时清除 PyInstaller 运行时环境变量（_PYI_*），新程序不再被误判为 onefile 子进程而拒绝启动
  - 更新脚本新增错误日志 update_error.log，方便排查
- 修正 v1.3.0 发布包内程序与 v1.2.0 相同的问题（已重新打包）

### 验证
- v1.3.1 的自动更新流程已本地端到端验证通过（检查更新 → 下载 → 替换 → 重启成功）

### 注意（重要）
- **v1.2.0 / v1.3.0 用户请手动下载 v1.3.1 一次**：旧版本程序内的自动更新逻辑存在缺陷
  （未清除 PyInstaller 运行时环境变量），无法自动升级到本版。安装 v1.3.1 后，
  之后的自动更新即恢复正常。

---

## [1.3.0] - 2026-08-25

### 新增
- 内置「插件商店」窗口（「导出」页 →「插件商店」）：
  - 读取主仓库 plugins.json 插件目录
  - 展示插件名称/版本/作者/说明
  - 一键下载安装到 plugins/ 目录，自动刷新加载
- 插件目录 plugins.json（schema_version 1）
- 插件仓库统一加 pwm-plugin 发现标签

---
## [1.2.0] - 2026-08-25（当前）

### 新增
- 插件设置项 API：api.add_setting(key, label, kind, default, options)
  （text / file / number / select / bool / range 滑块）
- 「插件设置」窗口：插件注册的设置项在此调整，保存到 config.json，重启保留
- 插件设置实时预览：滑块 / 文件 / 下拉 / 输入改动即时刷新预览
- 文字水印 + 图片水印可同时存在（插件水印样式叠加在文字水印之上）
- 图片水印插件：自定义图片（PNG/JPG/GIF，动图取第一帧静态）+ 10 张内置预设 GIF 水印
- 预设水印以缩略图画廊显示，点击选择
- 字体：支持子文件夹扫描、「刷新字体」按钮、打开下拉自动刷新、损坏字体自动回退
- 插件独立仓库模式（插件不再随本体发布，自由下载安装）
- 插件版本号机制：插件管理窗口显示每个插件的版本

### 修复
- 预设水印图缩略图点击无效（lambda 闭包变量捕获）
- 字体识别相关优化
- 自动更新替换失败（“Security validation failure”）：改用 VBS + ShellExecute，不再经过 cmd

### 其他
- 发布包 / 插件安装包按版本命名，releases/vX.Y.Z/ 分目录

---

## [1.1.0] - 2026-08-25

### 新增
- 插件 API 扩展（更强的可扩展性）：
  - `api.add_template_preset(name, template)` —— 插件可新增模板预设
  - `api.add_watermark_style(name, label, renderer)` —— 插件可自定义水印渲染样式
  - `api.on_export(func)` —— 导出前处理钩子（水印后、保存前可修改图像）
- 「样式」页新增「水印样式」下拉框（默认样式 + 插件样式）
- 示例插件更新：演示全部 6 种插件扩展点

---

## [1.0.0] - 2026-08-25（首个正式发布）
 - 2026-08-25（首个正式发布）

首个公开版本，包含以下全部功能：

### 功能
- 自动识别相机品牌型号（SONY ILCE-7CM2 → Sony A7C II，可手动覆盖）
- 自动检测快门 / 光圈 / ISO / 焦距 / 镜头 / 拍摄时间
- 支持 RAW（ARW/NEF/CR2/DNG 等，内嵌全尺寸预览，竖拍自动转正）
- 水印位置（九宫格 + 偏移 + 边距，默认贴底）/ 字号 / 颜色 / 字体 / 背景条 / 描边 / 阴影
- 模板预设 + 自定义模板（可保存），变量 {make} {model} {shutter} {aperture} {iso} 等
- 批量导出 JPG / PNG / WebP / BMP，JPG 可保留 EXIF
- 三种语言：简体中文 / English / 繁體中文（「导出」页一键切换）
- 自定义字体（fonts/ 文件夹或界面添加）
- 插件系统 + 插件管理窗口（自定义变量 / 相机名 / 导出格式）
- 热更新检查（config.json 配置 update_url）
- 设置自动保存（config.json）

### 其他
- 窗口标题显示版本号（Photo Watermark v1.0.0）
- 开源发布（MIT 许可证；作者 Shiraijikuu · AI 协助 OpenAI Codex）
